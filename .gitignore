# === Node dependencies (backend) ===
# These are recreated with npm install — no need to track them
backend/node_modules/

# === Python stuff ===
# Ignore Python cache files from your data scripts
__pycache__/
*.pyc

# === Logs and debug junk ===
# Generated automatically when things crash — don’t need to keep them
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Secret config files ===
# In case you add environment vars later (like API keys)
.env

# === OS clutter ===
# Mac and Windows love dropping these everywhere
.DS_Store
Thumbs.db

# === Raw data ===
# Keep your filtered clinics.json, but skip large input datasets
backend/data/Health_Center_Service_Delivery_and_LookAlike_Sites.csv
backend/data/hrsa_full.json  # if used again in the future

# === Optional junk ===
# Random backup, temp, or scratch files
*.bak
*.tmp
*.old